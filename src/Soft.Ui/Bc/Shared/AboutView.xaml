<UserControl x:Class="Soft.Ui.Bc.Shared.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Soft.Ui.Bc.Shared"
             mc:Ignorable="d" 
             d:DesignHeight="250" d:DesignWidth="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Text="{Binding ApplicationInfo, Mode=OneWay}" FontSize="12" FontWeight="Light" BorderThickness="0"/>
        <TextBox Grid.Row="1" Text="{Binding Copyrightinfo, Mode=OneWay}"   FontSize="12" FontWeight="Light" BorderThickness="0"/>
    </Grid>
</UserControl>
